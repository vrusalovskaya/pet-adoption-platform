databaseChangeLog:

  - changeSet:
      id: 003-users-reset-sequence
      author: victoria

      changes:

        - sql:
            sql: |
              SELECT setval(
                  'users.users_id_seq',
                  (
                      SELECT COALESCE(MAX(id), 1)
                      FROM users.users
                  )
              );