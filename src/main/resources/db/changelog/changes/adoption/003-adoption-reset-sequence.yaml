databaseChangeLog:

  - changeSet:
      id: 003-adoption-reset-sequence
      author: victoria

      changes:

        - sql:
            sql: |
              SELECT setval(
                  'adoption.adoption_applications_id_seq',
                  (
                      SELECT COALESCE(MAX(id), 1)
                      FROM adoption.adoption_applications
                  )
              );